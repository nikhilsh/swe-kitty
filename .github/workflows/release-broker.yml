name: release-broker

on:
  workflow_call:
    inputs:
      release_tag:
        description: Release tag/name to publish assets under
        required: true
        type: string

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  build-binaries:
    name: cross-compile broker
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    strategy:
      matrix:
        include:
          - { goos: linux,  goarch: amd64 }
          - { goos: linux,  goarch: arm64 }
          - { goos: darwin, goarch: amd64 }
          - { goos: darwin, goarch: arm64 }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'
      - uses: actions/setup-node@v4
        with:
          # Stage G: the broker spawns a Node sidecar (@xterm/headless)
          # to keep size-correct terminal grids per session. We install
          # its deps here so the release tarball ships the sidecar dir
          # alongside the Go binary.
          node-version: '20'

      - name: Build
        if: hashFiles('broker/go.mod') != ''
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: '0'
          RELEASE_TAG: ${{ inputs.release_tag }}
        working-directory: broker
        run: |
          mkdir -p dist
          go build -trimpath -ldflags "-s -w -X main.version=${RELEASE_TAG}" \
            -o dist/swe-kitty-broker-${GOOS}-${GOARCH} ./cmd/swe-kitty-broker

      - name: Install sidecar runtime deps
        if: hashFiles('broker/sidecar/package.json') != ''
        working-directory: broker/sidecar
        run: npm install --omit=dev

      - name: Bundle broker + sidecar tarball
        if: hashFiles('broker/sidecar/package.json') != ''
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        working-directory: broker
        run: |
          STAGE="dist/stage-${GOOS}-${GOARCH}"
          mkdir -p "${STAGE}/sidecar"
          cp "dist/swe-kitty-broker-${GOOS}-${GOARCH}" "${STAGE}/swe-kitty-broker"
          cp -R sidecar/sidecar.js sidecar/package.json sidecar/README.md sidecar/node_modules "${STAGE}/sidecar/"
          tar -C "${STAGE}" -czf "dist/swe-kitty-broker-${GOOS}-${GOARCH}.tar.gz" .

      - name: Upload binary to GitHub Release
        if: hashFiles('broker/go.mod') != ''
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ inputs.release_tag }}
        run: |
          gh release upload "${RELEASE_TAG}" \
            broker/dist/swe-kitty-broker-${{ matrix.goos }}-${{ matrix.goarch }} \
            --clobber

      - name: Upload broker+sidecar bundle to GitHub Release
        if: hashFiles('broker/sidecar/package.json') != ''
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ inputs.release_tag }}
        run: |
          gh release upload "${RELEASE_TAG}" \
            broker/dist/swe-kitty-broker-${{ matrix.goos }}-${{ matrix.goarch }}.tar.gz \
            --clobber

  upload-installer:
    name: upload install.sh
    runs-on: ubuntu-24.04
    needs: build-binaries
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Upload install.sh if present
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ inputs.release_tag }}
        run: |
          if [ -f scripts/install.sh ]; then
            gh release upload "${RELEASE_TAG}" scripts/install.sh --clobber
          else
            echo "no scripts/install.sh yet — skipping"
          fi
