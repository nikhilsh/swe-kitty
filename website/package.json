{
  "name": "swe-kitty-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "push-code": "npm run build && cd out && fyra push"
  }
}
